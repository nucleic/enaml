# .readthedocs.yml

version: 2

formats:
  - epub

build:
  tools:
    python: "3.9"

python:
  install:
     - requirements: docs/rtd_requirements.txt
     - requirements: docs/requirements.txt
     - method: pip
       path: .
